---
description: 前端开发规则（Vue 3 + TypeScript）
globs: ["Frontend/**/*.vue", "Frontend/**/*.ts", "Frontend/**/*.tsx", "Frontend/**/package.json"]
alwaysApply: false
---

# 前端开发规则

## 技术栈约束

- 框架：**Vue 3**，使用 **Composition API**，禁止使用 Options API
- 语言：**TypeScript**，所有文件必须为 `.ts` / `.vue`，禁止使用 `.js`
- 构建工具：**Vite**
- UI 组件库：**Element Plus**
- 图表：**ECharts**（通过 `vue-echarts` 封装使用）
- 状态管理：**Pinia**
- HTTP 请求：**Axios**（统一封装，禁止在组件中直接使用 `axios`）
- 路由：**Vue Router 4**

## 项目结构规范

```
Frontend/src/
├── api/          # 所有接口请求函数，按模块分文件
├── assets/       # 静态资源（图片、字体、全局样式）
├── components/   # 全局公共组件（需可复用）
├── composables/  # 组合式函数（useXxx 命名）
├── router/       # 路由配置
├── store/        # Pinia Store（按业务模块拆分）
├── types/        # 全局 TypeScript 类型定义
├── utils/        # 工具函数
├── views/        # 页面级组件（与路由一一对应）
└── main.ts
```

## 命名规范

- 组件文件：**PascalCase**，如 `TransactionList.vue`
- 页面文件：**PascalCase**，放于 `views/` 下，如 `DashboardView.vue`
- 组合式函数：**useXxx** 命名，如 `useTransaction.ts`
- Pinia Store：**useXxxStore** 命名，如 `useAccountStore.ts`
- API 函数：动词 + 名词，如 `getTransactions` / `createAsset`
- CSS 类名：**kebab-case**

## TypeScript 规范

- 禁止使用 `any`，所有接口响应数据必须定义类型（放于 `types/` 目录）
- API 响应统一使用泛型包装：
  ```typescript
  interface ApiResponse<T> {
    code: number
    message: string
    data: T
  }
  ```
- 金额相关数据在前端统一使用 `string` 类型接收（避免精度丢失），展示时格式化

## Axios 封装规范

- 统一在 `api/request.ts` 中创建 Axios 实例，配置 baseURL、超时、拦截器
- 请求拦截器：自动注入 JWT Token
- 响应拦截器：统一处理错误码，401 自动跳转登录页
- 所有 API 函数放于 `api/` 目录，禁止在组件或 Store 中直接调用 `axios`

## 组件规范

- 单文件组件（SFC）结构顺序：`<script setup>` → `<template>` → `<style scoped>`
- Props 必须定义类型，使用 `defineProps<{}>()` 泛型语法
- Emits 必须使用 `defineEmits<{}>()` 声明
- 避免在 `<template>` 中编写复杂逻辑，抽取为 `computed` 或方法
- 样式统一使用 `<style scoped>`，禁止全局样式污染（全局样式仅在 `assets/styles/` 中定义）

## 图表规范

- 所有 ECharts 图表封装为独立组件，放于 `components/charts/` 目录
- 图表组件需响应式适配，监听容器尺寸变化自动 resize
- 颜色主题统一在 `assets/styles/chart-theme.ts` 中定义

## 路由规范

- 使用路由懒加载：`() => import('./views/XxxView.vue')`
- 需鉴权页面统一添加路由守卫 meta：`{ requiresAuth: true }`
- 路由命名使用 **camelCase**

## 终端操作规范

- **禁止使用终端直接删除任何文件或目录**
- 依赖安装统一使用 `npm install`，禁止混用 yarn / pnpm（保持 lock 文件一致）
- 谨慎使用终端修改代码，优先通过编辑器直接修改文件
- 构建前执行 `npm run type-check` 确保无 TypeScript 错误
